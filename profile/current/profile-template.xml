<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
  <HostPackage>
    <Host autoClearCommandLineAfterSend="no" HighlightHistory="yes" printCommand="yes"
      USE_IRE_DRIVER_BUGFIX="no" mUSE_FORCE_LF_AFTER_PROMPT="no" mUSE_UNIX_EOL="no"
      mNoAntiAlias="no" mEchoLuaErrors="no" runAllKeyMatches="no" AmbigousWidthGlyphsToBeWide="auto"
      mRawStreamDump="no" mIsLoggingTimestamps="no" logDirectory="" logFileName=""
      logFileNameFormat="yyyy-MM-dd#HH-mm-ss" mAlertOnNewData="yes" mFORCE_NO_COMPRESSION="no"
      mFORCE_GA_OFF="no" mFORCE_SAVE_ON_EXIT="yes" mEnableGMCP="yes" mEnableMSSP="yes"
      mEnableMSP="yes" mEnableMSDP="no" mMapStrongHighlight="no" mEnableSpellCheck="yes"
      mEnableUserDictionary="yes" mUseSharedDictionary="no" mAcceptServerGUI="yes"
      mAcceptServerMedia="yes" mMapperUseAntiAlias="yes" mMapperShowRoomBorders="yes"
      mFORCE_MXP_NEGOTIATION_OFF="no" mFORCE_CHARSET_NEGOTIATION_OFF="no" enableTextAnalyzer="no"
      mRoomSize="0.5" mLineSize="10.0" mBubbleMode="no" mMapViewOnly="yes" mShowRoomIDs="no"
      mShowPanel="yes" mHaveMapperScript="no" mEditorAutoComplete="yes" mEditorShowBidi="yes"
      mEditorTheme="Mudlet" mEditorThemeFile="Mudlet.tmTheme" mThemePreviewItemID="-1"
      mThemePreviewType="" mSearchEngineName="" mTimerSupressionInterval="" mUseProxy="no"
      mProxyAddress="" mProxyPort="0" mProxyUsername="" mProxyPassword="" mSslTsl="no"
      mSslIgnoreExpired="no" mSslIgnoreSelfSigned="no" mSslIgnoreAll="no" mDiscordAccessFlags="3071"
      mRequiredDiscordUserName="" mRequiredDiscordUserDiscriminator="" mSGRCodeHasColSpaceId="no"
      mServerMayRedefineColors="no" playerRoomPrimaryColor="#ffff0000"
      playerRoomSecondaryColor="#ffffffff" playerRoomStyle="0" playerRoomOuterDiameter="120"
      playerRoomInnerDiameter="70" CompactInputLine="no" mDoubleClickIgnore="&quot;'"
      EditorSearchOptions="0" DebugShowAllProblemCodepoints="no" NetworkPacketTimeout="300">
      <name>__PROFILE_NAME__</name>
      <mInstalledPackages />
      <url>server.com</url>
      <serverPackageName>nothing</serverPackageName>
      <serverPackageVersion>-1</serverPackageVersion>
      <port>23</port>
      <borderTopHeight>0</borderTopHeight>
      <borderBottomHeight>0</borderBottomHeight>
      <borderLeftWidth>0</borderLeftWidth>
      <borderRightWidth>0</borderRightWidth>
      <wrapAt>100</wrapAt>
      <wrapIndentCount>0</wrapIndentCount>
      <mFgColor>#c0c0c0</mFgColor>
      <mBgColor>#000000</mBgColor>
      <mCommandFgColor>#717100</mCommandFgColor>
      <mCommandBgColor>#000000</mCommandBgColor>
      <mCommandLineFgColor>#808080</mCommandLineFgColor>
      <mCommandLineBgColor>#000000</mCommandLineBgColor>
      <mBlack>#000000</mBlack>
      <mLightBlack>#808080</mLightBlack>
      <mRed>#800000</mRed>
      <mLightRed>#ff0000</mLightRed>
      <mBlue>#000080</mBlue>
      <mLightBlue>#0000ff</mLightBlue>
      <mGreen>#008000</mGreen>
      <mLightGreen>#00ff00</mLightGreen>
      <mYellow>#808000</mYellow>
      <mLightYellow>#ffff00</mLightYellow>
      <mCyan>#008080</mCyan>
      <mLightCyan>#00ffff</mLightCyan>
      <mMagenta>#800080</mMagenta>
      <mLightMagenta>#ff00ff</mLightMagenta>
      <mWhite>#c0c0c0</mWhite>
      <mLightWhite>#ffffff</mLightWhite>
      <mDisplayFont>Bitstream Vera Sans Mono,14,-1,5,50,0,0,0,1,0</mDisplayFont>
      <mCommandLineFont>Bitstream Vera Sans Mono,14,-1,5,50,0,0,0,0,0</mCommandLineFont>
      <mCommandSeparator>;;</mCommandSeparator>
      <commandLineMinimumHeight>30</commandLineMinimumHeight>
      <mFgColor2>#c0c0c0</mFgColor2>
      <mBgColor2>#000000</mBgColor2>
      <mRoomBorderColor>#c0c0c0</mRoomBorderColor>
      <mMapInfoBg>#969696</mMapInfoBg>
      <mBlack2>#000000</mBlack2>
      <mLightBlack2>#808080</mLightBlack2>
      <mRed2>#800000</mRed2>
      <mLightRed2>#ff0000</mLightRed2>
      <mBlue2>#000080</mBlue2>
      <mLightBlue2>#0000ff</mLightBlue2>
      <mGreen2>#008000</mGreen2>
      <mLightGreen2>#00ff00</mLightGreen2>
      <mYellow2>#808000</mYellow2>
      <mLightYellow2>#ffff00</mLightYellow2>
      <mCyan2>#008080</mCyan2>
      <mLightCyan2>#00ffff</mLightCyan2>
      <mMagenta2>#800080</mMagenta2>
      <mLightMagenta2>#ff00ff</mLightMagenta2>
      <mWhite2>#c0c0c0</mWhite2>
      <mLightWhite2>#ffffff</mLightWhite2>
      <mSpellDic>en_US</mSpellDic>
      <mLineSize>10.0</mLineSize>
      <mRoomSize>0.5</mRoomSize>
      <mapInfoContributor>Short</mapInfoContributor>
      <profileShortcut key="Script editor">Alt+E</profileShortcut>
      <profileShortcut key="Show Map">Alt+M</profileShortcut>
      <profileShortcut key="Compact input line">Alt+L</profileShortcut>
      <profileShortcut key="Preferences">Alt+P</profileShortcut>
      <profileShortcut key="Notepad">Alt+N</profileShortcut>
      <profileShortcut key="Package manager">Alt+O</profileShortcut>
      <profileShortcut key="Module manager">Alt+I</profileShortcut>
      <profileShortcut key="MultiView">Alt+V</profileShortcut>
      <profileShortcut key="Play">Alt+C</profileShortcut>
      <profileShortcut key="Disconnect">Alt+D</profileShortcut>
      <profileShortcut key="Reconnect">Alt+R</profileShortcut>
      <profileShortcut key="Close profile">Alt+W</profileShortcut>
      <stopwatches />
    </Host>
  </HostPackage>
  <TriggerPackage />
  <TimerPackage />
  <AliasPackage>
    <Alias isActive="yes" isFolder="no">
      <name>Run Tests</name>
      <script>if not bustedState.isBustedAvailable() then
        print("Warning: Package \"busted\" not found.")
        return
        end
        if (not matches[2]) or (#matches[2] == 0) then
        bustedState.setup()
        else
        bustedState.setup({matches[2]})
        end
        bustedState.runTests()</script>
      <command></command>
      <packageName></packageName>
      <regex>^runTests(?: (.*))?$</regex>
    </Alias>
  </AliasPackage>
  <ActionPackage />
  <ScriptPackage>
    <Script isActive="yes" isFolder="no">
      <name>test scripts</name>
      <packageName></packageName>
      <script>-- Checks to see if the "busted" package is available.

        bustedState = bustedState or {}
        function bustedState.isBustedAvailable()
        if package.loaded["busted"] then
        return true
        else
        for _, searcher in ipairs(package.searchers or package.loaders) do
        local loader = searcher("busted")
        if type(loader) == 'function' then
        return true
        end
        end
        return false
        end
        end

        -- BEGIN Busted init section

        local options = {}
        -- options.output = os.getenv("BUSTED_OUTPUT") or 'treeOutput'
        options.output = os.getenv("BUSTED_OUTPUT") or 'plainTerminal'
        options.suppressPending = false
        options.language = "en"
        options.deferPrint = false
        options.verbose = false
        options.quitOnError = false
        options.recursive = true
        options.defaultPatterns = {"_spec"}

        if bustedState.isBustedAvailable() then
        local path = require 'pl.path'
        end
        options.defaultFiles = options.defaultFiles or {"."}

        bustedState.verbose = options.verbose
        bustedState.recursive = options.recursive
        bustedState.patternsTable = bustedState.patternsTable or options.defaultPatterns
        bustedState.filesTable = bustedState.filesTable or options.defaultFiles

        if bustedState.isBustedAvailable() then
        busted = busted or require 'busted.core'()

        if not bustedState.initRun then
        require 'busted'(busted)

        local quitOnError = options.quitOnError

        busted.subscribe({ 'error', 'output' }, function(element, parent, message)
        print(appName, ': error: Cannot load output library: ', element.name)
        print(message)
        return nil, true
        end)
        busted.subscribe({ 'error', 'helper' }, function(element, parent, message)
        print(appName, ': error: Cannot load helper script: ', element.name)
        print(message)
        return nil, true
        end)
        busted.subscribe({ 'error' }, function(element, parent, message)
        busted.skipAll = quitOnError
        return nil, true
        end)
        busted.subscribe({ 'failure' }, function(element, parent, message)
        busted.skipAll = quitOnError
        return nil, true
        end)

        local outputHandlerLoader = require 'busted.modules.output_handler_loader'()
        outputHandlerLoader(busted, options.output, {
        defaultOutput = options.output,
        verbose = options.verbose,
        suppressPending = options.suppressPending,
        language = options.language,
        deferPrint = options.deferPrint,
        arguments = {},
        })
        end
        bustedState.initRun = true
        end

        -- END busted init section

        function bustedState.setup( filesTable, patternsTable )
        if not bustedState.isBustedAvailable() then
        print("Warning: Package \"busted\" not found.")
        return
        end
        if type(filesTable) == "table" then
        if #filesTable &gt; 0 then
        local fallback = bustedState.filesTable
        bustedState.filesTable = {}
        for _, v in ipairs(filesTable) do
        if type(v) == "string" then
        table.insert(bustedState.filesTable, v)
        else
        bustedState.filesTable = fallback
        break
        end
        end
        else
        bustedState.filesTable = options.defaultFiles
        end
        end

        if type(patternsTable) == "table" then
        if #patternsTable &gt; 0 then
        local fallback = bustedState.patternsTable
        bustedState.patternsTable = {}
        for _, v in ipairs(patternsTable) do
        if type(v) == "string" then
        table.insert(bustedState.patternsTable, v)
        else
        bustedState.patternsTable = fallback
        break
        end
        end
        else
        bustedState.patternsTable = options.defaultPatterns
        end
        end

        if bustedState.setupRun then
        local oldctx = busted.context.get()
        busted.context.clear()
        local ctx = busted.context.get()
        for k, v in pairs(oldctx) do
        ctx[k] = v
        end
        root = busted.context.get()
        busted.safe_publish('suite', { 'suite', 'reset' }, root, 1, 1)
        end
        bustedState.setupRun = true

        bustedState.testFileLoader = bustedState.testFileLoader or require 'busted.modules.test_file_loader'(busted,
        {'lua'})
        bustedState.testFileLoader(bustedState.filesTable, bustedState.patternsTable, {
        excludes = {},
        verbose = bustedState.verbose,
        recursive = bustedState.recursive,
        })

        bustedState.execute = bustedState.execute or require 'busted.execute'(busted)
        end

        function bustedState.runTests()
        if not bustedState.isBustedAvailable() then return end

        if bustedState.runTestsRun then
        local tablex = require 'pl.tablex'
        local root = busted.context.get()
        local children = tablex.copy(busted.context.children(root))
        local oldctx = busted.context.get()
        busted.context.clear()
        local ctx = busted.context.get()
        for k, v in pairs(oldctx) do
        ctx[k] = v
        end
        for _, child in ipairs(children) do
        for descriptor, _ in pairs(busted.executors) do
        child[descriptor] = nil
        end
        busted.context.attach(child)
        end
        root = busted.context.get()
        busted.safe_publish('suite', { 'suite', 'reset' }, root, 1, 1)
        end
        bustedState.runTestsRun = true

        oldiowrite = io.write
        io.write = function (...)
        echo(...)
        end

        bustedState.execute(1, {})

        io.write = oldiowrite
        end</script>
      <eventHandlerList />
    </Script>
    <Script isActive="yes" isFolder="no">
      <name>quitMudletAfterTests</name>
      <packageName></packageName>
      <script>function quitMudletAfterTests()
        local quitMudlet = os.getenv("QUIT_MUDLET_AFTER_TESTS")
        if quitMudlet then
        ioprint("All tests complete, shutting down Mudlet")
        -- closeMudlet on its own inside an event handler causes a crash
        tempTimer(0, closeMudlet)
        end
        end</script>
      <eventHandlerList>
        <string>testsComplete</string>
      </eventHandlerList>
    </Script>
    <Script isActive="yes" isFolder="no">
      <name>autoRunTestsEvent</name>
      <packageName></packageName>
      <script>function autoRunTestsEvent()
        tempTimer(
        1,
        function()
        autoRunTests()
        end
        )
        end

        function autoRunTests()
        local runTests = os.getenv("AUTORUN_BUSTED_TESTS")
        if not runTests then
        ioprint("Not setup to auto-run tests.")
        return
        end

        ioprint("Setup to auto-run tests - commencing.")

        if not bustedState.isBustedAvailable() then
        ioprint("Busted not available - double-check that it's installed properly.")
        raiseEvent("testsComplete")
        return
        end

        local testLocation = os.getenv("TESTS_DIRECTORY")
        if not testLocation then
        ioprint("No location for the tests provided, don't know what to run.")
        raiseEvent("testsComplete")
        return
        end

        local preTestPackage = os.getenv("PRETEST_PACKAGE")
        if preTestPackage and preTestPackage ~= "" and preTestPackage ~= "NONE" then
        installPackage(preTestPackage)
        end

        busted.subscribe(
        {'failure'},
        function(element, parent, message)
        logTestError()
        return nil, true
        end
        )

        busted.subscribe(
        {'error'},
        function(element, parent, message)
        logTestError()
        return nil, true
        end
        )

        bustedState.setup({testLocation})
        bustedState.runTests()
        raiseEvent("testsComplete")
        end

        local errorLogged = false
        function logTestError()
        if errorLogged then return end

        if getOS() == "linux" then
        local sentinel = os.getenv("SENTINEL") or "/tmp/busted-tests-failed"
        local f, err = io.open(sentinel, "w")
        if f then
        f:write("Lua busted tests failed")
        f:close()
        else
        ioprint("Tests failed, but could not write sentinel '"..sentinel.."': "..tostring(err))
        end
        else
        ioprint("Tests failed, but did not know how to log the error for "..getOS())
        end
        errorLogged = true
        end</script>
      <eventHandlerList>
        <string>sysLoadEvent</string>
      </eventHandlerList>
    </Script>
  </ScriptPackage>
  <KeyPackage />
  <VariablePackage>
    <HiddenVariables />
  </VariablePackage>
</MudletPackage>
